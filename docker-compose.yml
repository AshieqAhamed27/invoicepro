services:
  backend:
    build:
      context: ./invoicepro/backend
      dockerfile: Dockerfile
    container_name: invoicepro-backend
    restart: unless-stopped
    env_file:
      - ./invoicepro/backend/.env
    environment:
      NODE_ENV: production
      PORT: 5000
    ports:
      - "5000:5000"
